interface:
  display_name: "Agent Workflow Interaction"
  short_description: "Deliberate with users, then route execution"
  default_prompt: "Use $use-agent-workflow as a thoughtful user-facing adviser: work through unsettled intent before routing committed execution."
